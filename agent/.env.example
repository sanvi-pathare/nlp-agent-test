# Copy to `.env` and set your BMC automation credentials.
AUTOMATION_USER=your_username
AUTOMATION_PASS=your_password

# Optional: agent HTTP port (default 5001; avoid 5000 on macOS — AirPlay uses it)
# AGENT_PORT=5001

# Optional: Hugging Face token for faster model downloads
# HF_TOKEN=
